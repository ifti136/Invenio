import 'package:flutter/material.dart';

class AddOnTypesScreen extends StatelessWidget {
  const AddOnTypesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('ADD-ON TYPES')),
      body: const Center(child: Text('Add-On Types Management')),
    );
  }
}
